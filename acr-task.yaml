version: v1.0.0
steps:
  - build: >
      -t {{.Run.Registry}}/base-artifacts/node:{{.Run.ID}}
      -t {{.Run.Registry}}/base-artifacts/node:9-alpine
      -f ./Dockerfile
      .
  - push: 
    - "{{.Run.Registry}}/base-artifacts/node:{{.Run.ID}}"
    - "{{.Run.Registry}}/base-artifacts/node:9-alpine"
